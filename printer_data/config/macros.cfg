[gcode_macro _CLIENT_VARIABLE]
variable_use_custom_pos   : True
variable_custom_park_x    : 28.0
variable_custom_park_y    : 10.0
variable_custom_park_dz   : 10.0

variable_retract          : 1.0
variable_cancel_retract   : 5.0
variable_speed_retract    : 35.0
variable_unretract        : 1.0
variable_speed_unretract  : 35.0
variable_speed_hop        : 15.0
variable_speed_move       : 100.0

variable_park_at_cancel   : True
variable_park_at_cancel_x : 28.0
variable_park_at_cancel_y : 210

variable_use_fw_retract   : False
variable_idle_timeout     : 0
variable_runout_sensor    : ""

variable_user_pause_macro : ""
variable_user_resume_macro: ""
variable_user_cancel_macro: ""
gcode:

[gcode_macro START_PRINT]
description: Start print macro called from Orca Slicer
gcode:
    {% set BED = params.BED|default(60)|float %}
    {% set HOTEND = params.HOTEND|default(200)|float %}

    RESPOND TYPE=echo MSG="START_PRINT IS RUNNING - BED={BED} HOTEND={HOTEND}"

    ; Start bed and hotend heating, but do not wait yet
    M140 S{BED}
    M104 S{HOTEND}

    ; Basic setup
    G90                     ; Absolute positioning
    M83                     ; Relative extrusion 
    G92 E0                  ; Reset extruder

    ; Home printer
    G28

    ; Wait for temperatures
    M190 S{BED}
    M109 S{HOTEND}

    ; Level/align gantry
    Z_TILT_ADJUST

    ; Re-home Z after Z_TILT_ADJUST
    G28 Z

    ; Load compensation profiles
    BED_MESH_PROFILE LOAD=default
    SKEW_PROFILE LOAD=Calilantern
            
    ; Purge line in X direction
    G92 E0
    G1 Z2.0 F3000
    G1 X15 Y15 Z0.3 F5000.0
    G1 X215 Y15 Z0.3 F1500.0 E15
    G1 X215 Y15.4 Z0.3 F5000.0
    G1 X15 Y15.4 Z0.3 F1500.0 E15
    G92 E0

    ; Move over to prevent blob squish
    G1 Z2.0 F3000
    G1 X28 Y20 Z0.3 F5000.0

    ; Final positioning before print starts
    G92 E0
    G1 Z2.0 F3000
    G1 X28 Y20 Z0.3 F5000.0

[gcode_macro END_PRINT]
description: End print macro called from Orca Slicer
gcode:
    SAVE_GCODE_STATE NAME=END_PRINT_STATE

    ; Retract and lift safely
    G91
    M83
    G1 E-2 F1800

    {% set z_max = printer.toolhead.axis_maximum.z|float %}
    {% set z_now = printer.toolhead.position.z|float %}
    {% set z_lift = 10 if z_now < (z_max - 10) else (z_max - z_now) %}
    G1 Z{z_lift} F1200

    ; Park and present print
    G90
    G1 X28 Y210 F6000

    ; Shut down heaters and fan
    M104 S0
    M140 S0
    M106 S0

    ; Clear print compensation
    BED_MESH_CLEAR
    SET_SKEW CLEAR=1

    ; Motors off
    M84

    RESTORE_GCODE_STATE NAME=END_PRINT_STATE

[pause_resume]

[pause_resume]

[gcode_macro M600]
description: Filament change pause
gcode:
    PAUSE X=28 Y=210 Z_MIN=50


[gcode_macro UNLOAD_FILAMENT]
description: Heat nozzle and unload filament
gcode:
    {% set TEMP = params.TEMP|default(210)|float %}

    SAVE_GCODE_STATE NAME=UNLOAD_FILAMENT_STATE

    M104 S{TEMP}
    M109 S{TEMP}

    M83
    G92 E0

    ; Small push first to soften the tip
    G1 E5 F300

    ; Retract/unload filament
    G1 E-60 F900

    G92 E0

    RESTORE_GCODE_STATE NAME=UNLOAD_FILAMENT_STATE


[gcode_macro LOAD_FILAMENT]
description: Heat nozzle and load filament
gcode:
    {% set TEMP = params.TEMP|default(210)|float %}

    SAVE_GCODE_STATE NAME=LOAD_FILAMENT_STATE

    M104 S{TEMP}
    M109 S{TEMP}

    M83
    G92 E0

    ; Feed filament in
    G1 E40 F600

    ; Slow purge
    G1 E20 F180

    G92 E0

    RESTORE_GCODE_STATE NAME=LOAD_FILAMENT_STATE

[gcode_macro HEATSOAK_ZTILT_MESH]
description: Heat soak bed, run Z tilt, then calibrate bed mesh
gcode:
    {% set BED = params.BED|default(60)|float %}
    {% set SOAK = params.SOAK|default(20)|int %}
    {% set PROFILE = params.PROFILE|default("default") %}

    RESPOND TYPE=echo MSG="Heating bed to {BED}C."
    M140 S{BED}
    M190 S{BED}

    RESPOND TYPE=echo MSG="Homing printer."
    G28

    ; Move to a safe center-ish position during soak
    G90
    G1 Z10 F1200
    G1 X110 Y110 F6000

    RESPOND TYPE=echo MSG="Heat soaking for {SOAK} minutes."
    G4 P{SOAK * 60000}

    RESPOND TYPE=echo MSG="Running Z_TILT_ADJUST."
    Z_TILT_ADJUST

    RESPOND TYPE=echo MSG="Re-homing Z after Z tilt."
    G28 Z

    RESPOND TYPE=echo MSG="Clearing old mesh and running BED_MESH_CALIBRATE."
    BED_MESH_CLEAR
    BED_MESH_CALIBRATE PROFILE={PROFILE}

    ; Present bed forward when done
    G90
    G1 Z10 F1200
    G1 X28 Y210 F6000

    RESPOND TYPE=echo MSG="Heat soak, Z tilt, and bed mesh complete. Run SAVE_CONFIG if you want to save the new mesh."

[gcode_macro HEATSOAK_NB]
description: Non-blocking heatsoak (minutes-based): heats bed/hotend and soaks without blocking the console.
variable_active: 0
variable_bed: 0
variable_hotend: 0
variable_soak_m: 0            # minutes remaining
variable_started_soak: 0
variable_idle_prev: 0
variable_idle_tmp_m: 30       # temp idle timeout during heatsoak (MINUTES)
variable_tolerance: 1.0       # degrees C tolerance for "at temp"
gcode:
  {% set bed = params.BED|default(60)|int %}
  {% set hotend = params.HOTEND|default(150)|int %}
  {% set soak_m = params.SOAK|default(15)|int %}
  {% set idle_tmp_m = params.IDLE|default(printer["gcode_macro HEATSOAK_NB"].idle_tmp_m)|int %}
  {% set tol = params.TOL|default(printer["gcode_macro HEATSOAK_NB"].tolerance)|float %}

  # Save current idle timeout to restore later (from config) - this is in SECONDS
  {% set prev_idle_s = printer.configfile.settings.idle_timeout.timeout|int %}
  {% set idle_tmp_s = idle_tmp_m * 60 %}

  SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=active VALUE=1
  SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=bed VALUE={bed}
  SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=hotend VALUE={hotend}
  SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=soak_m VALUE={soak_m}
  SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=started_soak VALUE=0
  SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=idle_prev VALUE={prev_idle_s}
  SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=idle_tmp_m VALUE={idle_tmp_m}
  SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=tolerance VALUE={tol}

  RESPOND PREFIX=HEATSOAK MSG="Starting heatsoak (non-blocking): BED={bed} HOTEND={hotend} SOAK={soak_m}m. Idle timeout -> {idle_tmp_m}m (was {prev_idle_s}s)."
  M117 Heatsoak NB: heating...

  SET_IDLE_TIMEOUT TIMEOUT={idle_tmp_s}
  M140 S{bed}
  M104 S{hotend}

  # Kick off the ticker quickly for "heating" status; it will switch to 60s once soaking starts.
  UPDATE_DELAYED_GCODE ID=HEATSOAK_NB_TICK DURATION=2.0


[gcode_macro CANCEL_HEATSOAK]
description: Cancel non-blocking heatsoak, stop ticker, restore idle_timeout. Use OFF=1 to turn heaters off.
gcode:
  {% set off = params.OFF|default(0)|int %}
  {% set hs = printer["gcode_macro HEATSOAK_NB"] %}

  {% if hs.active|int == 1 %}
    RESPOND PREFIX=HEATSOAK MSG="Cancelling heatsoak..."
    # Stop state first (prevents tick from re-arming)
    SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=active VALUE=0
    SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=started_soak VALUE=0
    SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=soak_m VALUE=0

    # Stop the delayed ticker
    UPDATE_DELAYED_GCODE ID=HEATSOAK_NB_TICK DURATION=0

    # Restore idle timeout (seconds)
    {% set prev_s = hs.idle_prev|int %}
    SET_IDLE_TIMEOUT TIMEOUT={prev_s}

    {% if off == 1 %}
      M104 S0
      M140 S0
      RESPOND PREFIX=HEATSOAK MSG="Heaters turned OFF."
    {% else %}
      RESPOND PREFIX=HEATSOAK MSG="Heaters left at current targets (use OFF=1 to turn off)."
    {% endif %}

    M117 Heatsoak cancelled
  {% else %}
    RESPOND PREFIX=HEATSOAK MSG="No active heatsoak to cancel."
  {% endif %}

[delayed_gcode HEATSOAK_NB_TICK]
initial_duration: 0
gcode:
  {% set hs = printer["gcode_macro HEATSOAK_NB"] %}
  {% if hs.active|int == 0 %}
    UPDATE_DELAYED_GCODE ID=HEATSOAK_NB_TICK DURATION=0
  {% else %}
    {% set bed_t = printer.heater_bed.temperature|float %}
    {% set bed_s = printer.heater_bed.target|float %}
    {% set hot_t = printer.extruder.temperature|float %}
    {% set hot_s = printer.extruder.target|float %}
    {% set tol = hs.tolerance|float %}

    {% set bed_ok = (bed_s <= 0) or (bed_t >= (bed_s - tol)) %}
    {% set hot_ok = (hot_s <= 0) or (hot_t >= (hot_s - tol)) %}

    {% if bed_ok and hot_ok %}
      {% if hs.started_soak|int == 0 %}
        SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=started_soak VALUE=1
        RESPOND PREFIX=HEATSOAK MSG="Temps reached. Starting soak timer."
        M117 Heatsoak NB: soaking...
      {% endif %}

      {% if hs.soak_m|int > 0 %}
        # Decrement once per minute
        SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=soak_m VALUE={hs.soak_m|int - 1}

        {% set mins = hs.soak_m|int %}
        {% set bed_t_s = '%.1f' % bed_t %}
        {% set bed_s_s = '%.0f' % bed_s %}
        {% set hot_t_s = '%.1f' % hot_t %}
        {% set hot_s_s = '%.0f' % hot_s %}
        RESPOND PREFIX=HEATSOAK MSG="{mins} min remaining (Bed {bed_t_s}/{bed_s_s}, Hotend {hot_t_s}/{hot_s_s})."
        M117 Heatsoak NB: {mins}m left

        # Re-arm in 60 seconds
        {% if printer["gcode_macro HEATSOAK_NB"].active|int == 1 %}
          UPDATE_DELAYED_GCODE ID=HEATSOAK_NB_TICK DURATION=60.0
        {% else %}
          UPDATE_DELAYED_GCODE ID=HEATSOAK_NB_TICK DURATION=0
        {% endif %}
      {% else %}
        RESPOND PREFIX=HEATSOAK MSG="Heatsoak complete. Restoring idle_timeout."
        {% set prev_s = hs.idle_prev|int %}
        SET_IDLE_TIMEOUT TIMEOUT={prev_s}
        SET_GCODE_VARIABLE MACRO=HEATSOAK_NB VARIABLE=active VALUE=0
        UPDATE_DELAYED_GCODE ID=HEATSOAK_NB_TICK DURATION=0
        M117 Heatsoak complete
      {% endif %}
    {% else %}
      {% set bed_t_s = '%.0f' % bed_t %}
      {% set bed_s_s = '%.0f' % bed_s %}
      {% set hot_t_s = '%.0f' % hot_t %}
      {% set hot_s_s = '%.0f' % hot_s %}
      M117 Heating... B {bed_t_s}/{bed_s_s} H {hot_t_s}/{hot_s_s}

      # While heating, update a bit more often so you see progress (every 5 seconds)
      {% if printer["gcode_macro HEATSOAK_NB"].active|int == 1 %}
        UPDATE_DELAYED_GCODE ID=HEATSOAK_NB_TICK DURATION=5.0
      {% else %}
        UPDATE_DELAYED_GCODE ID=HEATSOAK_NB_TICK DURATION=0
      {% endif %}
    {% endif %}
  {% endif %}

[gcode_macro TEST_SPEED]
# Home, get position, throw around toolhead, home again.
# If MCU stepper positions (first line in GET_POSITION) are greater than a full step different (your number of microsteps), then skipping occured.
# We only measure to a full step to accomodate for endstop variance.
# Example: TEST_SPEED SPEED=300 ACCEL=5000 ITERATIONS=10

description: Test for max speed and acceleration parameters for the printer. Procedure: Home -> ReadPositionFromMCU -> MovesToolhead@Vel&Accel -> Home -> ReadPositionfromMCU

gcode:
    # Speed
    {% set speed  = params.SPEED|default(printer.configfile.settings.printer.max_velocity)|int %}
    # Iterations
    {% set iterations = params.ITERATIONS|default(5)|int %}
    # Acceleration
    {% set accel  = params.ACCEL|default(printer.configfile.settings.printer.max_accel)|int %}
    # Minimum Cruise Ratio
    {% set min_cruise_ratio = params.MIN_CRUISE_RATIO|default(0.5)|float %}
    # Bounding inset for large pattern (helps prevent slamming the toolhead into the sides after small skips, and helps to account for machines with imperfectly set dimensions)
    {% set bound = params.BOUND|default(20)|int %}
    # Size for small pattern box
    {% set smallpatternsize = SMALLPATTERNSIZE|default(20)|int %}
    
    # Large pattern
        # Max positions, inset by BOUND
        {% set x_min = printer.toolhead.axis_minimum.x %}
        {% if x_min < 0 %}
            {% set x_min = 0 %}
        {% endif %}
    
        {% set y_min = printer.toolhead.axis_minimum.y %}
        {% if y_min < 0 %}
            {% set y_min = 0 %}
        {% endif %}
    
        {% set x_min = x_min + bound %}
        {% set x_max = printer.toolhead.axis_maximum.x - bound %}
        {% set y_min = y_min + bound %}
        {% set y_max = printer.toolhead.axis_maximum.y - bound %}
    
    # Small pattern at center
        # Find X/Y center point
        {% set x_center = (printer.toolhead.axis_minimum.x|float + printer.toolhead.axis_maximum.x|float ) / 2 %}
        {% set y_center = (printer.toolhead.axis_minimum.y|float + printer.toolhead.axis_maximum.y|float ) / 2 %}
        
        # Set small pattern box around center point
        {% set x_center_min = x_center - (smallpatternsize/2) %}
        {% set x_center_max = x_center + (smallpatternsize/2) %}
        {% set y_center_min = y_center - (smallpatternsize/2) %}
        {% set y_center_max = y_center + (smallpatternsize/2) %}

    # Save current gcode state (absolute/relative, etc)
    SAVE_GCODE_STATE NAME=TEST_SPEED
    
    # Output parameters to g-code terminal
    { action_respond_info("TEST_SPEED: starting %d iterations at speed %d, accel %d" % (iterations, speed, accel)) }
    
    # Home and get position for comparison later:
        M400 # Finish moves - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/66
        G28
        # QGL if not already QGLd (only if QGL section exists in config)
        {% if printer.configfile.settings.quad_gantry_level %}
            {% if printer.quad_gantry_level.applied == False %}
                QUAD_GANTRY_LEVEL
                G28 Z
            {% endif %}
        {% endif %} 
        # Move 50mm away from max position and home again (to help with hall effect endstop accuracy - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/24)
        G90
        G1 X{printer.toolhead.axis_maximum.x-50} Y{printer.toolhead.axis_maximum.y-50} F{30*60}
        M400 # Finish moves - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/66
        G28 X Y
        G0 X{printer.toolhead.axis_maximum.x-1} Y{printer.toolhead.axis_maximum.y-1} F{30*60}
        G4 P1000 
        GET_POSITION

    # Go to starting position
    G0 X{x_min} Y{y_min} Z{bound + 10} F{speed*60}

    # Set new limits
    {% if printer.configfile.settings.printer.minimum_cruise_ratio is defined %}
        SET_VELOCITY_LIMIT VELOCITY={speed} ACCEL={accel} MINIMUM_CRUISE_RATIO={min_cruise_ratio}
    {% else %}
        SET_VELOCITY_LIMIT VELOCITY={speed} ACCEL={accel} ACCEL_TO_DECEL={accel / 2}
    {% endif %}

    {% for i in range(iterations) %}
        # Large pattern diagonals
        G0 X{x_min} Y{y_min} F{speed*60}
        G0 X{x_max} Y{y_max} F{speed*60}
        G0 X{x_min} Y{y_min} F{speed*60}
        G0 X{x_max} Y{y_min} F{speed*60}
        G0 X{x_min} Y{y_max} F{speed*60}
        G0 X{x_max} Y{y_min} F{speed*60}
        
        # Large pattern box
        G0 X{x_min} Y{y_min} F{speed*60}
        G0 X{x_min} Y{y_max} F{speed*60}
        G0 X{x_max} Y{y_max} F{speed*60}
        G0 X{x_max} Y{y_min} F{speed*60}
    
        # Small pattern diagonals
        G0 X{x_center_min} Y{y_center_min} F{speed*60}
        G0 X{x_center_max} Y{y_center_max} F{speed*60}
        G0 X{x_center_min} Y{y_center_min} F{speed*60}
        G0 X{x_center_max} Y{y_center_min} F{speed*60}
        G0 X{x_center_min} Y{y_center_max} F{speed*60}
        G0 X{x_center_max} Y{y_center_min} F{speed*60}
        
        # Small pattern box
        G0 X{x_center_min} Y{y_center_min} F{speed*60}
        G0 X{x_center_min} Y{y_center_max} F{speed*60}
        G0 X{x_center_max} Y{y_center_max} F{speed*60}
        G0 X{x_center_max} Y{y_center_min} F{speed*60}
    {% endfor %}

    # Restore max speed/accel/accel_to_decel to their configured values
    {% if printer.configfile.settings.printer.minimum_cruise_ratio is defined %}
        SET_VELOCITY_LIMIT VELOCITY={printer.configfile.settings.printer.max_velocity} ACCEL={printer.configfile.settings.printer.max_accel} MINIMUM_CRUISE_RATIO={printer.configfile.settings.printer.minimum_cruise_ratio} 
    {% else %}
        SET_VELOCITY_LIMIT VELOCITY={printer.configfile.settings.printer.max_velocity} ACCEL={printer.configfile.settings.printer.max_accel} ACCEL_TO_DECEL={printer.configfile.settings.printer.max_accel_to_decel}
    {% endif %}

    # Re-home and get position again for comparison:
        M400 # Finish moves - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/66
        G28 # This is a full G28 to fix an issue with CoreXZ - https://github.com/AndrewEllis93/Print-Tuning-Guide/issues/12
        # Go to XY home positions (in case your homing override leaves it elsewhere)
        G90
        G0 X{printer.toolhead.axis_maximum.x-1} Y{printer.toolhead.axis_maximum.y-1} F{30*60}
        G4 P1000 
        GET_POSITION

    # Restore previous gcode state (absolute/relative, etc)
    RESTORE_GCODE_STATE NAME=TEST_SPEED